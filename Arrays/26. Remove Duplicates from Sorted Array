
---

## 🟢 26. Remove Duplicates from Sorted Array  
**Arrays / Two Pointers**

```md
# 26. Remove Duplicates from Sorted Array

## 🔍 Intuition
- Sorted array → duplicates are adjacent
- Overwrite duplicates in-place

---

## 🧠 Approach (Two Pointers)
- `i` scans array
- `k` stores next unique element
- Write only when value changes

---
## ⏱ Complexity

Time: O(n) ,
Space: O(1)

## ✅ Python Code

```python
class Solution:
    def removeDuplicates(self, nums):
        if not nums:
            return 0

        k = 1
        for i in range(1, len(nums)):
            if nums[i] != nums[i - 1]:
                nums[k] = nums[i]
                k += 1

        return k
